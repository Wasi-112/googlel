// Function with default parameter
//function greet(name = 'Guest') {
  //console.log(`Hello ${name}!`);
//}

// Calling the function without passing a parameter
//greet(); // Output: Hello, Guest!

// Calling the function with a parameter
//greet('Alice');
//greet('wasi');
// Output: Hello, Alice!
// Function to calculate area of a rectangle with default parameters
//function calculateArea(length = 5, width = 3) {
  //return length * width;
//}

// Calling the function without passing any arguments
//const area1 = calculateArea();
//console.log(area1); // Output: 15 (default length = 5, default width = 3)

// Calling the function with arguments
//const area2 = calculateArea(6, 4);
//console.log(area2); // Output: 24 (length = 6, width = 4)

// Regular function expression
const multiply = function (a, b) {
  return a * b;
};

// Arrow function equivalent
const multiplyArrow = (3, 4) => 3 * 4;
